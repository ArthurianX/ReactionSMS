/*
Accounts.onCreateUser(function(options, user) {

    user.phone = '';
    user.contacts = [''];
    user.randomContact = [''];
    user.slots = 100;
    user.favoriteReactions = [''];
    user.statistics = {
        conversations: '',
        messages: '',
        randomMessages: '',
        reactionsStats: ['']
    };
    user.game = {
        dailyRandomMessageEnabled: 0,
        dailyRandomMessageUsed: 0,
        badges: ['']
    };


    // We still want the default hook's 'profile' behavior.
    if (options.profile)
        user.profile = options.profile;
    console.log(user);
    return user;
});*/
